# Performance Test Instructions — Midnight Evidence

**Note:** Minimal strategy does not require additional performance tests beyond unit tests.

The express scope uses the Minimal test strategy. Performance tests are generated for
Standard and Comprehensive strategies only.

NFR1 (60 fps) was validated as code structure in Code Generation using
`requestAnimationFrame` for the game loop.
